node_modules/
dist/
dist-mcp/
*.tsbuildinfo
.next/
.wrangler/
.vercel/
.env
.env.local
*.log
.DS_Store
.mcp.json
*.toml.bak
.env.production
.env.staging
# Wrangler local secrets — matches apps/worker/.dev.vars and any other worker's copy at any depth
.dev.vars
.dev.vars.*
apps/web/out/
apps/worker/dist/
.worktrees/
.claude/worktrees/
.line-harness-setup.json
.line-harness-config.json

# Wrangler local secrets — matches apps/worker/.dev.vars and any other worker's copy at any depth
.dev.vars
.dev.vars.*

# Claude Code local dev-server config (machine-local paths)
.claude/launch.json

# D1 の本番ダンプ (wrangler d1 export の出力)。友だち名・LINEユーザーID・
# メッセージ履歴といった実顧客データを含むため、このリポジトリは public で
# ある以上ワークツリーに置いても絶対にコミットしない。
line-harness-before-*.sql
*-d1-export*.sql
